# Phoenix LiveView stack

The app will use Elixir, Phoenix LiveView, and Postgres for the MVP. This matches the desired stack, keeps learner and admin flows server-rendered, and avoids adding a separate frontend application before the product needs one.
